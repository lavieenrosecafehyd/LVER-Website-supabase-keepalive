name: Lighthouse
on:
  schedule: [{cron: "0 5 * * 0"}]
  workflow_dispatch:
jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: treosh/lighthouse-ci-action@v12
        with:
          urls: |
            https://lavieenrosecafe.in
