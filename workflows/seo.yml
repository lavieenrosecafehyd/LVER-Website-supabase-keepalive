name: SEO Check
on:
  schedule: [{cron: "0 8 * * 1"}]
  workflow_dispatch:
jobs:
  seo:
    runs-on: ubuntu-latest
    steps:
      - run: |
          curl -L https://lavieenrosecafe.in > index.html
          grep -i "<title>" index.html
          grep -i "meta name=\"description\"" index.html
          grep -i "canonical" index.html
